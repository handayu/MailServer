MailService是一个windows服务，里面有定时器，特定执行外部程序MailProcessor去截图服务器然后发送邮件到QQ邮箱，可以作为一个独立的和其他外部程序交互，
关机之后重启会自动启动服务，一直执行，定时执行的时间可以在配置里面配置，外部程序执行的路径也可以，但是这个里面没写，定死在了代码里，可以提出来，
后面更改。

如果更改了时间和路径的配置，记得要卸载服务，然后重新安装服务，服务的卸载和安装命令写在txt说明里。

Service执行外部程序需要那个dll，已经在程序里。


另外，外部程序是console，右键项目属性改为了windows程序，可以隐藏了console窗口，这样截图不会有console挡住截图。当然也可以用它的windwosAPI隐藏窗口，但是好像，。
不行。